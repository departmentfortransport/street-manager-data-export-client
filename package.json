{
  "name": "street-manager-data-export-client",
  "version": "1.0.0",
  "description": "The client used to communicate with the street-manager-data-export-api",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/departmentfortransport/street-manager-data-export-client.git"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "tslint --project tslint.json",
    "clean": "rimraf dist",
    "build": "npm run clean && npm run test && tsc",
    "test": "npm run lint"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^0.19.2",
    "http-status-codes": "^1.4.0",
    "qs": "^6.5.2"
  },
  "devDependencies": {
    "@types/node": "^13.9.2",
    "@types/qs": "^6.9.1",
    "rimraf": "^3.0.2",
    "tslint": "^6.1.0",
    "typescript": "^3.8.3"
  }
}
